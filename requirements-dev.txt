# Test/dev dependencies — NOT installed in the prod runtime (keep the service
# image lean). CI (smoke.yml) installs these to run the unit suite; install
# locally with `pip install -r requirements-dev.txt` to run `pytest`.
pytest>=8,<9
ruff>=0.4,<1
